{
	"name": "desktop-agent",
	"version": "0.1.0",
	"private": true,
	"main": "dist/main.js",
	"scripts": {
		"dev": "ts-node-dev --respawn src/main.ts",
		"build": "tsc -p tsconfig.json && echo 'Package with electron-builder as needed'"
	},
	"dependencies": {
		"active-win": "^9.0.0",
		"axios": "^1.7.2",
		"electron": "^30.0.0",
		"keytar": "^7.10.0"
	},
	"devDependencies": {
		"concurrently": "^8.2.2",
		"ts-node-dev": "^2.0.0",
		"typescript": "^5.5.4"
	}
}